# Prompt Template: เขียนรายละเอียดโมดูล ERP ฟาร์ม ให้ dev อ่านแล้วเริ่มพัฒนาได้เลย

ใช้ prompt นี้ (แก้เฉพาะบรรทัดสุดท้าย) ทุกครั้งที่จะให้ AI เขียนไฟล์รายละเอียดโมดูลใหม่ เพื่อให้ได้ผลลัพธ์ที่โครงสร้าง/ดีไซน์/ความลึกสอดคล้องกันทุกไฟล์

---

บทบาท: คุณเป็นสถาปนิกระบบ ERP ที่กำลังเขียนสเปกโมดูลละเอียดให้ทีม dev ใช้พัฒนาโมดูล ERP สำหรับธุรกิจฟาร์ม (ครอบคลุมปศุสัตว์และพืชไร่/พืชสวน) ที่ต้อง deploy แบบ on-premise ได้

บริบทโปรเจกต์:
- เอกสารชุดนี้อยู่ในโฟลเดอร์ `คู่มือ ERP ฟาร์ม`
- ไฟล์ภาพรวม: `คู่มือ ERP สำหรับธุรกิจฟาร์ม.html`
- ไฟล์ตัวอย่างที่ทำเสร็จแล้ว ใช้เป็นต้นแบบโครงสร้างและดีไซน์ทุกจุด: `คู่มือบัญชีและการเงิน สำหรับ ERP ฟาร์ม.html`

งานที่ต้องทำ: เขียนไฟล์ HTML ใหม่ชื่อ `คู่มือ[ชื่อโมดูลภาษาไทย] สำหรับ ERP ฟาร์ม.html` สำหรับโมดูล **[ชื่อโมดูลไทย] ([Module Name English])**

## กฎการออกแบบ (ต้องตรงกับไฟล์ต้นแบบทุกจุด ห้ามคิดดีไซน์ใหม่)
1. คัดลอกทั้ง `<style>` block จากไฟล์ต้นแบบมาใช้ตรง ๆ (สี, ฟอนต์, dark-mode tokens, radius, shadow, `.phase-tag`, `.func-item`, `td.method`, `.entity-card`, `.field-table-wrap` พร้อม container-query สำหรับ mobile) — ห้ามเปลี่ยนพาเลตหรือฟอนต์
2. หัวข้อทุกระดับ (h1, h2, h3, หัวตาราง, nav.toc) เขียนรูปแบบ **"ไทย (English)"** เสมอ
3. มี breadcrumb ด้านบน เชื่อมกลับไปไฟล์ภาพรวมและหมวดโมดูลหลักที่เกี่ยวข้อง
4. มีปุ่ม "← กลับไปภาพรวม ERP ฟาร์ม" ที่ footer
5. ตารางฟิลด์ในการ์ดเอนทิตี (ส่วนฐานข้อมูล) ต้องมี container query (`.entity-card{container-type:inline-size;}` + `@container (max-width:500px){...stack เป็น label/value...}`) ที่คัดลอกมาจากไฟล์ต้นแบบ เพื่อไม่ให้ตารางล้นการ์ดแคบ — **ห้ามใช้ Bootstrap หรือ framework ภายนอกใด ๆ** เพราะ Artifact ที่ publish บล็อกการโหลดจาก CDN และจะทำลายดีไซน์ที่สอดคล้องกันทั้งชุดเอกสาร

## โครงสร้างเนื้อหา — ต้องมีครบ 7 ส่วนนี้ เรียงตามลำดับ (เน้น dev อ่านแล้ว implement ได้เลย ไม่ใช่เอกสารมาร์เก็ตติ้ง)

1. **ขอบเขตและจุดเชื่อมต่อ (Scope & Integration Points)**
   - การ์ด "อยู่ในขอบเขต" กับ "อยู่นอกขอบเขต (รับข้อมูลจากโมดูลอื่น)"
   - ตารางจุดเชื่อมต่อ: โมดูลต้นทาง | เหตุการณ์ที่ทำให้เกิดข้อมูล | สิ่งที่โมดูลนี้ต้องทำ | Phase

2. **ฟังก์ชันหลัก (Core Functions)**
   - การ์ด "MVP — ต้องมีวันแรก (Phase 1)" ลิสต์เฉพาะฟังก์ชันที่จำเป็นจริง ๆ ให้ใช้งานได้
   - การ์ด "เพิ่มทีหลังได้" ลิสต์ฟีเจอร์ที่ผัดไปทำทีหลังได้ แต่ละข้อระบุ Phase 2/3 กำกับ (ใช้ `.phase-tag`)

3. **ฐานข้อมูล (Database)**
   - แยกเป็น 1 การ์ดต่อ 1 เอนทิตี (ไม่ใช่ตารางสรุปรวมบรรทัดเดียว) แต่ละการ์ดมี:
     - ชื่อเอนทิตี (snake_case ภาษาอังกฤษ) + phase tag ที่หัวการ์ด
     - คำอธิบาย 1-2 ประโยคว่าเอนทิตีนี้แทนอะไรในโลกจริง (ไม่ใช่แค่ชื่อ table)
     - บรรทัด "ความสัมพันธ์" อธิบายว่าผูกกับเอนทิตี/โมดูลอื่นอย่างไร
     - ตารางฟิลด์ย่อย 3 คอลัมน์: **ฟิลด์** | **ชนิดข้อมูล** (ข้อความ/ตัวเลข/วันที่/จริง-เท็จ/ตัวเลือกคงที่ (enum) พร้อมลิสต์ค่าที่เลือกได้/เลขอ้างอิง FK→ตารางไหน) | **เก็บอะไร** (อธิบายเป็นภาษาคนอ่านเข้าใจ ไม่ใช่แค่ทวนชื่อฟิลด์)
   - ต้องมีฟิลด์ที่สะท้อนความเป็นฟาร์มจริง (ผูก batch, cost center ต่อแปลง/โรงเรือน ฯลฯ) ไม่ใช่ generic schema ทั่วไป
   - ห้ามเขียนฟิลด์แบบลิสต์คำคั่นจุลภาคในช่องเดียว (เช่น "code, name, type, ...") ต้องแตกเป็นแถวตารางแยกทีละฟิลด์เสมอ เพื่อให้คนที่ไม่ใช่ dev อ่านเข้าใจว่าแต่ละฟิลด์คืออะไร

4. **API**
   - ตาราง endpoint: Method | Path (มี `/resource` ชัดเจน) | คำอธิบาย | Phase
   - ครอบคลุม endpoint พื้นฐาน CRUD ของเอนทิตีหลัก + endpoint รายงานที่ระบุไว้ในฟังก์ชันหลัก
   - endpoint ที่โมดูลอื่นเรียกเข้ามา (เช่น auto-post journal entry) ให้ระบุว่าเป็น internal endpoint

5. **สิทธิ์การเข้าถึง (Roles & Permissions)**
   - role-card อย่างน้อย 3-4 บทบาทที่เกี่ยวข้องกับโมดูลนี้จริง (ห้าม copy บทบาทจากโมดูลอื่นแบบไม่คิด)
   - ตาราง permission matrix: การกระทำ (แถว) × role (คอลัมน์) พร้อม ✓/✗ หรือเงื่อนไข เพื่อออกแบบ RBAC ได้ตรง ๆ

6. **สูตรคำนวณและกฎทางธุรกิจ (Business Rules & Formulas)**
   - การ์ดละ 1 สูตร/กฎ ที่เป็นตรรกะจริงเบื้องหลังฟังก์ชัน/เอนทิตีในส่วน 2-3 (ไม่ใช่แค่พูดถึงฟีเจอร์ซ้ำ) เช่น เกณฑ์การจัดสรร, ตัวคูณ/หาร, threshold, bucket, กฎการรับรู้รายการ
   - แสดงสูตรด้วย `<pre class="formula">...</pre>` (pseudocode/สมการอ่านง่าย ไม่ต้องเป็นโค้ดภาษาใดภาษาหนึ่ง) ใช้ `&lt; &gt; &#215;` แทนสัญลักษณ์ที่ชนกับ HTML
   - ถ้ามีตัวอย่างตัวเลขประกอบ ใส่เป็น `<p class="formula-example">` ต่อท้ายสูตร
   - สูตรไหนต้องอาศัยดุลยพินิจของผู้เชี่ยวชาญเฉพาะทาง (บัญชี/สัตวแพทย์/เกษตร) ให้ใส่ `.note` เตือนว่าควรให้ผู้เชี่ยวชาญยืนยันก่อนใช้จริง อย่านำเสนอเป็นคำตอบสุดท้าย
   - ห้ามข้ามส่วนนี้แม้โมดูลจะดูเหมือนไม่มีการคำนวณ — อย่างน้อยต้องมีกฎ validation หรือ threshold บางอย่างเสมอ

7. **หน้าจอที่ต้องมี (Screens/Pages)**
   - 1 การ์ดต่อ 1 หน้าจอ ใช้ class เดิม (`card func-card`) ไม่สร้าง CSS ใหม่ — ชื่อหน้า (ไทย (English)) + phase tag ที่หัวการ์ด, คำอธิบาย 1 ประโยค, แล้วลิสต์ "ส่วนประกอบหลัก" ด้วย `<ul class="func-list"><li class="func-item"><span class="txt">...</span></li></ul>`
   - แต่ละหน้าระบุ: ตาราง/ฟิลด์หลักที่แสดง, ตัวกรอง/ค้นหาที่ต้องมี, ปุ่ม/การกระทำหลัก, และลิงก์ไปหน้าอื่นถ้ามี — สืบจาก entity/endpoint/role ที่กำหนดไว้ในส่วน 2-5 เท่านั้น ห้ามเดาฟีเจอร์ใหม่ที่ไม่มี entity/endpoint รองรับ
   - ครอบคลุมหน้าหลักของทุก Phase (ไม่ใช่แค่ Phase 1) แต่ระดับรายละเอียดพอ "รายชื่อหน้า + ส่วนประกอบหลัก" ไม่ต้องลงถึง wireframe/ตำแหน่ง component บนจอ
   - เพิ่มลิงก์ไปหัวข้อนี้ใน `nav.toc` ด้วย (`<a href="#pages">7. หน้าจอที่ต้องมี (Screens)</a>`)

## ข้อกำหนดเนื้อหา
- เขียนให้ dev อ่านแล้วออกแบบ database/API ต่อได้จริง ห้ามเป็น high-level มาร์เก็ตติ้ง — ทุกฟีเจอร์ต้องสืบไปหา entity/endpoint ที่รองรับได้
- อ้างอิงมาตรฐาน/หน่วยงานไทยที่เกี่ยวข้องจริงถ้ามี (เช่น กรมปศุสัตว์, กรมวิชาการเกษตร, มกอช., GAP/GMP, กรมสรรพากร/e-Tax)
- ระบุ dependency ชัดเจนว่าฟิลด์/ฟีเจอร์ไหนต้องรอข้อมูลจากโมดูลอื่นก่อน (โยงกับตารางจุดเชื่อมต่อในส่วนที่ 1)
- ห้ามใส่ฟีเจอร์ที่ไม่เกี่ยวกับโมดูลนี้โดยตรง
- ความลึกเทียบเท่าไฟล์ต้นแบบ (บัญชีและการเงิน) — อย่าเขียนสั้นกว่าจนขาดรายละเอียดที่ dev ต้องใช้

## หลังเขียนเสร็จ
- เพิ่มลิงก์ "ดูรายละเอียดโมดูลนี้ →" ในการ์ดที่เกี่ยวข้องของไฟล์ภาพรวม (`คู่มือ ERP สำหรับธุรกิจฟาร์ม.html`)
- บันทึกไฟล์ในโฟลเดอร์ `คู่มือ ERP ฟาร์ม` เดียวกัน

## โมดูลที่ต้องการให้เขียนตอนนี้
[ใส่ชื่อโมดูล เช่น "จัดซื้อ (Procurement)"]
